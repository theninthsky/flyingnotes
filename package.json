{
  "name": "flying-notes",
  "version": "1.0.0",
  "description": "an application for notes management.",
  "main": "dist/server.js",
  "scripts": {
    "start": "tsc && node ./dist/server.js",
    "dev": "tsc -w & nodemon ./dist/server.js",
    "client": "cd client && npm start",
    "app": "cd client && npm run build && cd .. && npm start",
    "heroku-postbuild": "cd client && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Almog Gabay",
  "license": "ISC",
  "dependencies": {
    "@types/cors": "^2.8.6",
    "@types/express": "^4.17.2",
    "@types/mongoose": "^5.5.30",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.7.9",
    "typescript": "^3.7.2"
  },
  "devDependencies": {
    "nodemon": "^1.19.4"
  }
}
